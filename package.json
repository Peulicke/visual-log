{
    "name": "visual-log",
    "version": "0.1.0",
    "main": "dist/index.js",
    "scripts": {
        "build": "tsc -p tsconfig.dist.json",
        "deploy": "tsc -p tsconfig.dist.json && npm publish",
        "start": "react-scripts start"
    },
    "dependencies": {
        "three": "^0.143.0"
    },
    "devDependencies": {
        "@types/three": "^0.143.1",
        "eslint-plugin-only-warn": "^1.0.3",
        "react": "^18.2.0",
        "react-scripts": "^5.0.1",
        "typescript": "^4.7.4"
    },
    "browserslist": {
        "production": [
            ">0.2%",
            "not dead",
            "not op_mini all"
        ],
        "development": [
            "last 1 chrome version",
            "last 1 firefox version",
            "last 1 safari version"
        ]
    }
}
